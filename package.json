{
  "name": "template-ts-app-node",
  "version": "1.0.0",
  "description": "Template for TypeScript based Web application",
  "main": "index.js",
  "scripts": {
    "build": "tsc sources/index.ts --outDir ./dist",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pushan Mitra",
  "license": "MIT",
  "devDependencies": {
    "awesome-typescript-loader": "^3.4.1",
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "source-map-loader": "^0.2.4"
  }
}
